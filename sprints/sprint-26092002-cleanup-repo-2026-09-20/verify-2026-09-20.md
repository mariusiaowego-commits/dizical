---
sprint: sprint-26092002-cleanup-repo
date: 2026-09-20
verifier: orchestrator pane w10:pA (MiniMax-M3)
method: read-only review (minimax pane w10:pB) + 实际 git 校验
---

# Verify — sprint-26092002-cleanup-repo

## 验证前状态 (主仓 /Users/mt16/dev/dizical, 2026-09-20 11:30 CST)
```
HEAD = origin/main = 2aee3fb
git status --porcelain = 11 条目
git status --porcelain -uall = 83 文件 (1 M + 82 ??)
git worktree list = 4 条 (含 1 prunable)
git branch -r (non-main) = 4 条
/tmp 相关 sprint 26091901 文件 = 16
```

## 验证点 (PR 推送前必走)

### 1. 3 commit 完成后必空
```bash
git status --porcelain          # 期望: 空
git log --oneline -3            # 期望: 3 个 chore 提交
git show --stat HEAD~2 HEAD~1 HEAD  # 期望: 13 / 63 / 10 文件
```

### 2. 每个 commit 内部一致性
- **Commit 1 chore(sprint)**: 13 文件, 全在 `sprints/` 下
- **Commit 2 chore(docs)**: 63 文件, 6 md + 57 png, `docs/screenshots/uiux-audit-2026-09-08/` 目录必空 (即 `git status --porcelain docs/screenshots/uiux-audit-2026-09-08` 为空)
- **Commit 3 chore(dizical-ai)**: 10 文件, `dizical-ai/node_modules/` + `dizical-ai/.cloudrun-deploy/` + `dizical-ai/logs/` 必不入 commit

### 3. 0 产品代码改动
```bash
git diff main..HEAD -- src/ tests/  # 期望: 0 改动
git show --stat HEAD~2 HEAD~1 HEAD  # 期望: 仅 sprints/ docs/ dizical-ai/ + .gitignore + .dockerignore
```

### 4. .gitignore 门 (PR 内 0 误跟踪)
```bash
git ls-files | grep -E "docs/handoff-archive/|STATUS.md"  # 期望: 空
git ls-files | grep "dizical-ai/node_modules\|dizical-ai/\.cloudrun-deploy\|dizical-ai/logs"  # 期望: 空
```

### 5. STATUS.md 工作区修订成功 (不入 commit)
```bash
grep -nE "MCP cloudbase auth 过期|deploy 阻塞" STATUS.md  # 期望: 0 命中
grep -nE "Deploy #132 已 9-19 12:20:40 全量切流" STATUS.md  # 期望: 1 命中
git status --porcelain STATUS.md  # 期望: 空 (因 .gitignore)
```

### 6. /tmp 清理后 (本地操作, 不入 commit)
```bash
ls /tmp/sprint-26091901-*.md 2>/dev/null | wc -l  # 期望: 7 (段 2a 过程稿删后, 9 个归档走 docs/, 4 个 keep 留)
ls /tmp/sprint-26091901-agy-*.md /tmp/sprint-26091901-warden-*.md 2>/dev/null | wc -l  # 期望: 0 (全归档走)
ls docs/handoff-archive/sprint-26091901-*.md 2>/dev/null | wc -l  # 期望: 9 (归档完成)
ls /tmp/trash-26092002/ 2>/dev/null | wc -l  # 期望: 19 (段 2 删件进 trash)
```

### 7. worktree prune
```bash
git worktree list  # 期望: 3 条 (主仓 + hygiene-260917 + timer-260917, verify / 已 prune)
git worktree list --porcelain | grep prunable  # 期望: 空
```

### 8. PR 推送后 (等 dad review)
- gh pr view 看到 chore(repo) PR
- PR head = chore/cleanup-repo-260920
- PR body 含 6 问 review packet: 改了什么 / 不该变什么 / 文件清单 / 怎么知道没问题 / 风险 / 回滚
- branch protection: dad 必须 review+merge, 不自主 merge

### 9. 风险标注 (review packet 必含)
- 仓库 +7.7M 截图 (从 368M → ~376M)
- .gitignore 多 1 行 (dizical-ai/.cloudrun-deploy/)
- 0 产品代码改动
- 0 push 远端 origin/main (走 PR 流程)

## 验收签字
- [ ] orchestrator 自验 1-7 通过
- [ ] gh pr create 成功
- [ ] PR URL 发 dad
- [ ] 等 dad review+merge
- [ ] closeout: 补齐 vault 7 doc 剩余 5 件 + tag + Obsidian md5
