# sprint-26091602-oracle-list-scaling-and-footer-refine-2026-09-16

**父 sprint**：sprint-26091601-oracle-ccg-card-redesign-2026-09-16（PR #330 已 merge main `ca5363e`；PR #331 文档归档 `44981b7`）

**发起人**：dad（在 `localhost:8765` 真机验收 PR #330 合流后的典藏卡，连续三轮提出打磨要求）

**目标**：针对典藏卡在**列表态**的呈现细节与性能表现做三轮收敛 —— 缩放过大的名牌 / 图形 / 印章，建立类别与单字映射，清除卡片外层白框，隐藏 Modal 内遮挡典故的锁标，限制成就页展示数量，并补懒加载与闲置降帧。

**范围（三轮，共 12 项改动）**

**首轮（7 项 UI/UX 问题）**
1. 列表态 `.oracle-nameplate` 收紧高度 / 内边距 / 字号（原过高遮挡人物原画）
2. `.oracle-pin` / `.oracle-seal` / `.oracle-avatar` 按卡宽等比收缩至 ~15–16px
3. 页脚类别动态化 —— 单字映射：突破→破、巅峰→极、执着→韧、段位→阶、晋级→跃、神秘→秘、主题→典；头像随类别动态
4. 移除 `.badge-card`（badges.html）与 `.b-card`（achievements.html）双层白底框 / 边框 / 内边距
5. 锁定卡片在详情 3D Modal 放大 / 翻转时隐藏锁标
6. 成就页已解锁列表限制展示数量
7. cache-buster 升级，避免静态资源缓存

**二轮（4 项视觉与性能）**
8. 水墨名牌**文字左对齐**
9. 立绘**重心居中微调**
10. badges 卡墙**自适应多列**（原固定断点）
11. **懒加载 + 列表墙闲置降帧**

**三轮（1 项）**
12. 成就页展示数量由 3 张调整为 5 张

**非范围（硬约束）**

- 后端零改动：`routes/*`、`badge_db.py`、`badge_theme.py` 不动
- `data/`、pytest、依赖清单不动；无新增依赖
- 3D 几何参数基线不做破坏性变更
- untracked 运行时产物不纳入提交

**验收线**

| # | 验收项 | 目标线 |
|---|--------|--------|
| 1 | 列表态名牌 | 不遮挡人物原画 |
| 2 | 图形 / 印章 / 头像 | 按卡宽等比，不再偏大 |
| 3 | 页脚类别 | 7 类各有单字映射，无 hardcode |
| 4 | 外层白框 | `.badge-card` 与 `.b-card` 双清，盲盒不受影响 |
| 5 | Modal 锁标 | 详情态不遮挡卡背典故 |
| 6 | 成就页数量 | 仅展示最近 5 张，总数统计保留 |
| 7 | 网格布局 | 宽屏自适应铺满，手机 2 列 |
| 8 | 懒加载 / 降帧 | 图片 `loading="lazy"`，列表墙闲置降帧 |
| 9 | 资源版本 | cache-buster 统一，4 个引用点同步 |
| 10 | 回归测试 | `pytest -q --ignore=tests/config_ui_fixes` → 768 passed / 0 failed |
| 11 | 改动隔离 | 仅 6 个前端文件；无后端 / DB 污染 |

**实施顺序**

1. 首轮：6 文件改动 + 回归 + CDP 视觉验收 → commit `4496757`
2. 二轮：名牌左对齐 / 立绘重心 / 网格自适应 / 懒加载降帧 → commit `248bf43`
3. 三轮：展示数量 3 → 5 → commit `3daf66f`
4. dad 拍板「准备推进」→ PR #332 squash merge → main `5ef98fa`
5. 切回 main ff 同步 + 回归复跑 + 本地服务重启 + 文档收尾

**Alternative considered and rejected**：仅调整 `--card-w` 缩小整张卡来「顺带」解决名牌过大 —— 拒绝，因为 dad 的诉求是**名牌相对卡面的比例**过大，整体缩卡会牺牲原画可读性且不解决比例问题。
