---
id: 26091201
type: sprint
version: 1.7.0
start_date: 2026-09-12
end_date: 2026-09-13
status: 进行中
priority: 高
summary: "徽章 CCG v2：info-bar/plate 信息区重设计 + 背景主题系统(每套卡可配置配色/纹理/图案)。agy(Opus 4.6) 出方案，hermes + minimax 双开发 agent 实现，hermes 调度+review"
tags: [sprint, dizical, badge, ccg, theme]
---

# SPRINT 26091201 — 徽章 CCG v2 (信息区重设计 + 背景主题系统)

## 0. 来源 (dad 2026-09-12 原话)

1. `ccg-info-bar` 和 `ccg-plate` 我觉得还需要重新设计，给 https://poke-holo.simey.me/ 上的内容作为参考，
   去发给 agy 让 agy 出我一个策略方案，但它不要写代码 只是让它给方案。然后你来严格参照开发。
2. 背景深紫色 是不是需要 plan 单独作为以后每一套卡片不同的可配置的颜色、纹理、图案。
   我看到 pokemon 的卡片不同的主题都有不同的风格和纹理以及背景色。只有深紫色肯定是不够的。
   这一块也让 agy 做一次 plan，你双写记录下 plan 后作为开发项目开发。
3. agy 现在写 plan，你分配工作让你和 minimax 作为开发 agent 进行完美参照 plan 的实现。然后你来调度和 review。

## 1. Goal

- **A 线 (信息区重设计)**：`.ccg-info-bar` + `.ccg-plate` 按 poke-holo 参考重做版式/层级/间距/配色
- **B 线 (背景主题系统)**：背景从硬编码深紫，升级为「每套主题 = 配色 + 纹理 + 图案 + 镭射光谱」的可配置系统

## 2. dad 拍板 (2026-09-12, 4 问)

| # | 决策 | 结论 |
|---|------|------|
| Q1 | PR #323 怎么走 | **v2 继续叠在当前分支**，最后一起 merge 一个大 PR（不新切分支） |
| Q2 | A-1 是否新增属性条 | **新增**（殿堂 / 类别 / 年份 三列，银条与说明栏之间） |
| Q3 | A-4 描述行数 | **3 行**（`-webkit-line-clamp: 3`，行高 1.55） |
| Q4 | B-1 主题来源 | **`achievements` 加列 + 未设时按分类兜底映射**（灵活且自动） |

## 3. 分工 (hermes 调度)

| 角色 | 职责 | 本轮任务 |
|------|------|---------|
| agy (w19:p1, Opus 4.6) | 出方案 (只出方案, 不写代码) | 已交付 `/tmp/agy-ccg-plan-260912.md` (505 行 / 8 决策点) |
| hermes (w19:p3) | 调度 + review + A 线实现 + sprint 文档 | A 线(信息区/属性条) + 主题 token 接线 + JS 接线 + 文档/CHANGELOG + B-1 review |
| minimax (w19:p2) | B 线实现 (按文件所有权切分, 与 hermes 零重叠) | B 线资产(已交付) + B-1 后端(已交付) |

## 4. 文件所有权 (防冲突, 同一 checkout)

- **hermes**: `src/kid_app/static/css/badge-ccg.css`、`src/kid_app/static/css/badge-ccg-themes.css`(接线)、`src/kid_app/static/js/badge-ccg.js`、`docs/**`、`API-CHANGELOG.md`、sprint 文档
- **minimax**: `src/kid_app/badge_theme.py`(new)、`badge_db.py`、`database.py`、`badge_draft.py`、`routes/badge_claim.py`、`routes/minip_api.py`、`app.py`(badges payload + startup hook)、`schema_mysql.sql`(+`.cloudrun-deploy` 副本)、`tests/test_badge_theme.py`(new)、`tests/` 6 个 fixture

## 5. 关键事实修正 (实施期发现)

- **agy 方案 B.4 映射表用错列**：`achievements.category` 实际只有 `milestone` / `seasonal` 两值（家族字段），
  人读分类在 **`achievements.type`**（突破 23 / 段位 10 / 执着 4 / 巅峰 4 / 晋级 2 / 神秘 1，共 44）。
  → **兜底映射按 `type`**：突破→azure、执着·段位→bamboo、巅峰·晋级→coral、神秘→imperial、`category='seasonal'`→frost、其余 azure。
- **镭射色偏**：plan 的逐段 `--sunpillar-1~6` 覆盖太脆，实装改为每主题一个 `--theme-laser-tint`（filter 表达式）。
- **暗纹**：外部 SVG `static/img/ccg-patterns/<theme>.svg`；security 层 mask 已从 `--ccg-security-*` 切到 `--theme-security-*`（否则换主题暗纹不变）。
- **本地 8765 跑的是云 MySQL**（`DATABASE_URL` 注入，`src/database.py:1734` 分流）：MySQL 后端**不走** `_init_tables`,
  ⇒ 任何新列的迁移必须在别处触发（本轮补 `app.py` startup hook 调 `ensure_card_theme_column`）。

## 6. 状态

- [x] 建 sprint 骨架 (repo + vault 双写)
- [x] agy 方案 A/B 交付 + 双写入库 (`docs/AI-PLAN-ccg-v2-260912.md`, md5 一致)
- [x] item 1 镭射方框修复 (`6bef848`, 消融剖面取证)
- [x] A 线信息区重设计 (银条三列 / 标题单菱形 / 日期中文格式 / 描述 3 行)
- [x] A-1 属性条新增 (`top 88.4% h 3.1%`, 三端实测无重叠/不裁字)
- [x] B 线主题资产 (5 套变量块 + 4 个 pattern SVG + demo 切换)
- [x] 主题接线 (`data-ccg-theme` + security mask 走 `--theme-*`)
- [x] B-1 后端 `card_theme` 列 + 兜底映射 + 3 处 payload + API-CHANGELOG (minimax 实施, hermes review PASS)
- [x] 验证: pytest 7 文件 **94 passed** + 独立探针 `resolve_card_theme` 12/12 + 云端 payload 46 行全带 `card_theme`
- [x] A 线回退后转 demo 迭代 → 定稿: 8 套主题 (5 深 azure/bamboo/coral/imperial/frost + 3 淡 pearl/mint/sakura) + 画面窗/金框几何 8 主题复验 (§11)
- [x] 深色主题「两个圆角矩形一淡一深」2 根因修复 (§11)
- [x] `card_stars` 后端列 + 全链路接线 (dad image#9 ④, API-CHANGELOG §1.3/1.4)
- [x] demo 版本化: `freeze-ccg-demo.sh` + `static/demo-archive/v1.0.0/` 冻结 (dad 2026-09-13 要求, §12.1)
- [x] dad 拍板 l2「固定」→ 淡色主题金框固定金色; l4/l5 各出对比版; l1 大白话重述 (§12.2)
- [ ] dad 拍板: L1 画面窗方案 / L4 底座去留 / L5 防伪档位 / 对比页真机验收 (§12.3)
- [ ] P1 卡编号 (dad 已答「需要」, 按 PRD 方案 A 开工前待确认 Q1/Q2/Q4 默认值)
- [ ] 收尾 (handoff / wiki / dad 真机验收) + 大 PR merge

## 7. 验收纪律 (回归不可退)

1. v1 修复必须保持：指针出窗镭射无直角硬边 (`6bef848`)；光斑滑出金框不压深色 (`8af7ccc`)。
2. 三端 (mac 1440 / iPad 竖 744 / iPad 横 1133) 0 console 报错 + 下半区零重叠。
3. 视觉终验交 dad 真机，agent 只出数值探针与截图。

## 8. B-1 交付与 review 结论 (2026-09-12)

**交付物 (minimax)**: `badge_theme.py`(VALID_CARD_THEMES / TYPE_THEME_MAP / resolve_card_theme) + 双后端
`ensure_card_theme_column` + `achievements.card_theme` 三源同步 (SQLite/MySQL/测试 fixture) + `badge_draft.py`
严格校验 (脏值 reject) + 三处 payload 解析 + `tests/test_badge_theme.py`。

**P0 发现与修复 (hermes review)**:
- 本地 8765 直连**云端腾讯 MySQL**, 表缺 `card_theme` 列 ⇒ 新代码 SELECT 立即 `pymysql 1054` 500。
- 根因: `_init_tables` 只属 SQLite 单例; MySQL 无迁移触发点。
- 修法: `app.py` startup hook (`ensure_card_theme_column` 双后端幂等), 与 CloudRun 上线同路径。
- 实测: 服务重启后 `/api/achievements` **200 / 46 行全部带 card_theme** (azure 25 / bamboo 14 / coral 6 / imperial 1)。

**已知 caveat (不阻塞, 留待后续)**:
- `frost` 走 type 路径永不触发 (6 类 type 全命中映射)，只有 `category='seasonal'` 且 type 缺失才落到 frost。
- 云端 DDL 走 MCP 留 `requestId` 需 dad device 授权（本轮 MCP 返 `AUTH_REQUIRED` ⇒ 按 dad 规则走 fallback：app 启动迁移 + 日志留痕）。

## 9. hermes 独立 review 抓到 2 个真失败 (已修, commit `491531e`)

minimax 自报的 **58 passed 是其自选子集**，漏了 `tests/test_badge_db_mysql_compat.py`；hermes 跑**全量** pytest 暴露：

1. `test_insert_achievement_row_pymysql_uses_positional` — 断言 `%s` 计数 = 15，新增列后是 16 ⇒ 期望更新为 16 列 + `card_theme in sql` + `len(params) == 16`。
2. `test_update_badge_current_pymysql` — `badge_write_tx` 内新增的 `ensure_card_theme_column` 自己 `commit()` 一次 ⇒ 写事务变 2 次 commit。

**修法 (改生产码，不松测试)**: 删 `_ensure_card_theme_mysql` 里多余的 `conn.commit()` — MySQL DDL **隐式提交**（ALTER 本身已生效），提交责任归还调用方（写事务 / 启动 hook），与 SQLite 分支语义一致。

**补测 (此前零覆盖的分支)**: MySQL 迁移分支 3 条 — 列已存在→只探 `information_schema` 不发 ALTER / 缺列→发 ALTER 且不自行 commit / 表不存在→早返回不 ALTER。踩坑: 探针取的是**整表列清单**，空 rows 会被判成"表不存在"而非"缺列"，mock 必须给非空列清单才测得到 ALTER 路径。

**最终验收: 全量 `pytest tests/` 730 passed / 8 skipped / 0 failed**（726 基线 + 4 新测试）。

## 10. 回退: 撤信息区重设计 + 属性条 (dad 真机否决, commit `76541e9`)

**dad 原话**: 「回退到上一版，这版完全不行」，三条原因:
- ① badge 主视觉周围多了一圈边框 → 与卡片外框一起变成「边框套边框」
- ② 外框圆角与主视觉圆角角度不一致 → 主视觉圆角矩形漏出来（截图 image#4）
- ③ 卡片高度太小 → 纵向挤在一起，整张卡变成矮胖型

**回退范围 (dad 拍板: 只撤新信息区 + 属性条，保留 5 套主题)**
- 撤: `badge-ccg.js`（银条单菱形 ornament / `ccg-bar-info`+`ccg-bar-rarity` 拆分 / 中文日期 / `ccg-attr-bar` / 星标尺寸）
  + `badge-ccg.css`（`--bar-h/--plate-t/--plate-b/--foot-b` 几何、银条内边距字号、说明栏圆角/内边距/标题字号/描述 3 行 clamp/页脚配色/星标尺寸、`.ccg-attr-bar` 块 + `--attr-t/--attr-h`）
- 保: item 1 镭射 clamp（4 处 `background-position`）、5 套主题（`badge-ccg-themes.css` + `ccg-patterns/*.svg` + `data-ccg-theme`）、B-1 后端 `card_theme`
- 副作用: A-4 的「描述 3 行」随信息区一起回到 2 行；如需 3 行是单点改动

**回退方法**: 以 `git show 6bef848:<file>` 为基准重建 → 只贴回主题必需接线（`--theme-*` token 契约块 + 4 主题 pattern 选择器 + glow/frame/stock/laser/security/plate 改 `--theme-*`），
脚本 `/tmp/ccg_rollback.py` 每处替换断言命中次数（避免静默漏改）→ 追加**回退 commit**（不 reset，PR #323 历史保持可回溯）。
踩坑: 批量 `replace_all` 把 token 块里的 `--theme-security-pattern: var(--ccg-security-pattern)` 也改成了自引用（无限循环值），复检时抓到并修回 —— 定义行与引用行不能一起批量替换。

**顺带修功能死角**: `badge-ccg-themes.css` 此前**只有 demo 页引用**，真卡页面（领取弹窗 `_badge_claim_modal.html`）加载不到 → 补 1 行 `<link>`，否则 5 套主题在真卡上等于不存在。

**取证结论 (关键, 防误判)**: ①/②/③ 的规则来源**不是 A 线引入** —— `.ccg-art-frame` 规则（`::after` inset 2px / radius 12px、内窗 inset 6px / radius 9px）与 `--card-w/--card-h`（= 1.4 比）在 `6bef848` 与回退前**逐字节相同**。
⇒ 这三点在 dad 认可的那一版里同样存在，或来自截图中的其它元素（如 badge 图 PNG 自带圆角/白底）。**需 dad 在截图上标注具体元素**（或给选择器）才能定位修复，不猜。

## 11. demo 迭代期 → 定稿 (dad 真机 12 轮反馈, 2026-09-12 夜)

**模式切换**: A 线回退后 dad 转入「只出 demo 反复看样式」——本轮**禁 commit / 禁跑测试流程**，逐轮真机截图反馈
(`clip_…1327 5` ~ `clip_…2152 12`)，agent 只做数值探针 (Chrome CDP 9444 + PIL)。

| # | dad 反馈 | 处置 | 落点 |
|---|---------|------|------|
| 5–8 | 全息幅度 / 聚光灯 / 防伪纹 / 淡色主题缺失 | 主题 token 化 + **新增 3 套淡色主题** (pearl/mint/sakura) + demo `?theme=` 8 套切换 | `badge-ccg-themes.css` / `badge-ccg-demo.html` |
| 9 | ① 镭射方框 ② `ccg-info-bar` 与模块不同宽 ③ badge 名未居中 ⑤⑥ star 位置 / 背面殿堂名 ④ **「star 是 hard code 还是有后端字段?」** | ①②③⑤⑥ 前端改 (`--art-l` 对齐内容栏) + **④ 新增 `card_stars` 后端列全链路** | `badge-ccg.css` / 后端 9 文件 |
| 10 | frame 左右紧贴卡边 | 先做金框左右内缩 → **dad 判「越改越错」** | (已撤) |
| 11 | ① 头部标签/编号贴边 ② `ccg-foil-stack` 左右贴卡边 ③ 整卡一圈很粗的黄描边 (所有主题) | ①③ = 金框内缩露出暗色卡身的副作用 ⇒ **`.ccg-frame` 回 `inset: 0`** (贴卡最外缘)；② = 真该改的是画面窗 ⇒ `--ccg-art-inset: 11px → var(--art-l) 5.4%` + `--ccg-rim-inner-r = R − 窗内缩量` (同心) | `badge-ccg.css` |
| 12 | 复核 | 8 主题 CDP 数值复验: 画面窗 / 信息条同一竖线 (≈5.7%)、金框 L/R = 0 | 探针 |

**深色主题「两个圆角矩形一淡一深」2 根因 (已修)**:
1. `.ccg-foil-stack` 本体 + 防伪纹 `::before` 用 18px 圆角却嵌在 7px 画面窗内 ⇒ 四角裁出新月形裸卡身 ⇒ 加窗口作用域半径规则 (18px→7px)。
2. `--theme-glare-stops` 第 3 个色标 `hsla(180,11%,35%,1) 60%` 硬切 ⇒ 改低透明 + 渐隐到 100%。

**定稿层级口径**: 金框 `inset:0` 贴卡最外缘 → 一圈宽度均匀的卡身衬底 → 画面窗从 5.4% (= 内容栏) 起。
token 默认值 = 深色现值 ⇒ 3 套淡色主题零回归 (azure mint/pearl token 逐字节未动)。

**存底**: `260af14` (后端 card_stars) + `aabcb32` (前端 8 主题/几何) + 本笔 (sprint §11 + `API-CHANGELOG.md` §1.3/1.4) — 仍在 PR #323 内，**未 merge**。

### 11.1 遗留待 dad 拍板 (阻塞收尾)

| # | 问题 | 现状 |
|---|------|------|
| L1 | 画面窗是 **1.217 长方形** vs 主体画 **512×512 方形** ⇒ 左右各 ~41px 只显示卡身衬底 | 四选项: A 放大画填满窗 (上下裁 ~9%) / B 窗收成方形 / C 窗内加主题暗底 / D 先不动 |
| L2 | 淡色主题的金框现在**跟主题变色** (mint 变绿 / sakura 变粉) | dad 曾说「金框保持金色系」⇒ 待定是否把 3 个 frame token 固定回金色 |
| L3 | 上下两条金环是否也内缩 (缩需 foot 上移 2px) | 左右已统一，上下仍贴卡上下缘 |
| L4 | 金框自带 2px 暗色掐丝底座 (`#342205`) 是否保留 | 非本轮新增，一行可去 |
| L5 | 防伪纹强度偏小 / 是否接生产 (需加 `card_sec` 列 + 领取弹窗写 `data-ccg-sec`) | 未擅自动 |

**另有独立 PRD 待拍板**: `docs/AI-PRD-徽章卡编号-260912.md` (卡面 `No.007` 现无后端来源，前端兜底 `001`；推荐 A 方案 = `achievements.card_no` 列 + 回填 44 + `MAX+1`)。

> ⬆ 上表 5 项 L1–L5 已于 2026-09-13 由 dad 拍板，处置见 **§12**。

## 12. demo 版本化 + dad 2026-09-13 拍板 (l1–l5 / P1)

### 12.1 demo 版本化 (dad 原话: 「保留 demo 的 html 文件到本地…要知道目前自己是第几个版本」)

- **机制**: `bash scripts/freeze-ccg-demo.sh <版本号> "说明"` ⇒ 生成自包含快照 `src/kid_app/static/demo-archive/<版本号>/`
  (`index.html` + `badge-ccg.css` + `badge-ccg-themes.css` + `badge-ccg.js` + `patterns/*.svg` + `MANIFEST.txt` 记 md5/commit/日期)，
  并把一行追加进 `static/demo-archive/VERSIONS.md`。**旧版本永不覆盖**（脚本遇同名直接报错退出）。
- **规则 (以后改卡片/3D 样式必须遵守)**: 先升版本号 → 改 `badge-ccg.css` / `badge-ccg.js` 文件头 + demo 页头 chip + `<meta name="ccg-demo-version">`
  → 定稿后再 `freeze` 冻结。任何时刻打开 demo 页，右上角就写着当前是第几版。
- **已冻结**: **v1.0.0** = dad 真机验收的定稿 (commit `82d52b1`)，入口 `/static/demo-archive/v1.0.0/index.html`。
- **当前开发版**: **v1.3.0-dev** (二轮: 跟手聚光灯旋钮 `--ccg-light-gain`=0.6 减弱 40% / 页脚并入说明栏，见 §14)。v1.2.0-dev（l1 铺满整卡 + 金环 6px/3 环 + 外框简化 + 文字区主题化对比，§13）与 v1.1.0-dev 均已并入本版，未单独冻结。
- 快照里外部资产（徽章原图 PNG、默认暗纹 data-URI）仍按 `/static/...` 绝对路径取**线上**文件 —— 样式/3D 文件本身是自包含的，这是刻意的（只冻结"设计"）。同理 `MANIFEST.txt` 里记了每个文件的 md5。

### 12.2 dad 拍板结果 → 处置

| # | 问题 | dad 答复 | 处置 |
|---|------|---------|------|
| L1 | 画面窗是 1.217 长条 vs 主视觉 512×512 方图 ⇒ 左右各 ~41px 只有卡身衬底 | 「没理解」→ 后升级为**新需求**（铺满整卡 / 去一层框 / 金环减细 / 外框简化 / 文字区主题化） | ✅ **已实施 v1.2.0-dev**，见 §13。原 A/B/C/D 四选项被此新方向**否决** |
| L2 | 淡色主题金框跟主题变色 (mint 变绿 / sakura 变粉) | **「固定」** | ✅ 已实施：pearl / mint / sakura 取消 3 条 ring 覆盖（回落 `#342205`/`#d4a017`/`#4e3207`），内圈 hi/lo 也收回金色系 |
| L3 | 「上下两条金环要不要内缩」 | 「没看到金环是什么样子」 | ✅ 出可视化：对比页 ① 把环带整圈涂洋红 + 左上角 3× 放大；口径改写为「金环 = 整圈金属边（同心环叠成）」。**l1 后带宽 11px → 6px（对比页自动跟随 `--ccg-rim-w`）** |
| L4 | 金框自带最外 1px 暗色掐丝底座 (`#342205`) 去/留 | 「各出一版让我看对比」 | ✅ 出对比：`data-ccg-base="off"` 开关（同几何、只把最外一圈改透明），对比页 ② 并排。**l1 后该环 2px → 1px** |
| L5 | 防伪强度偏小怎么办 / 接生产怎么做 | 「你准备怎么做？」 | ✅ 出旋钮 `--ccg-sec-gain`（同时乘常驻暗纹与聚光灯彩虹）+ 对比页 ③ 三档 ×1 / ×1.6 / ×2.4；接生产方案见 §12.5 |
| P1 | 卡编号 | **「需要」** → 随后 **「写进后续 plan 里，不在这次的 sprint 里做了，这次内容太多了」** | ⏩ **移出本 sprint**，登记为 B1 → `docs/AI-PLAN-ccg-backlog-260913.md`（方案 A 全文仍在 `docs/AI-PRD-徽章卡编号-260912.md`） |

### 12.3 本轮实现 (v1.1.0-dev)

- `badge-ccg.css`: 新增 `--ccg-sec-gain`（l5 防伪强度总增益，默认 1 = 现值）、`--ccg-frame-base-c`（l4 底座色，可置 `transparent`）、demo 版本号 chip 样式；文件头加版本号与升号规则
- `badge-ccg-themes.css`: l2 —— 3 套淡色主题的金框固定金色（token 覆盖删除，回落默认金）
- `badge-ccg-compare.html` (新): 四段对比 —— ① 金环在哪（标注 + 放大）② 底座 留/去 并排 ③ 防伪强度 ×1/×1.6/×2.4 ④ 淡色主题金框
- `badge-ccg-demo.html` / `badge-ccg.js`: 版本号 chip + 文件头版本；demo 页加了「细节对比页」入口
- `scripts/freeze-ccg-demo.sh` (新) + `static/demo-archive/` (v1.0.0 快照 + VERSIONS.md)
- 机械自检 (Chrome CDP 9444，只取数值/布尔，视觉交 dad): 9 张卡全部挂载（9 stage / 18 frame / 9 front）；
  金环标注 `rgba(233,30,220,.45) 11px`；底座留 = `rgb(52,34,5) 2px` vs 底座去 = `rgba(0,0,0,0) 2px`（几何同）；
  防伪 `::before` opacity **0.2 → 0.32 → 0.48**（= 0.2 × 增益）；mint/sakura 的 `--theme-frame-gold` 已无声明（回落默认金，与 azure 同色）；
  两页 console 0 错误 + 无 HTTP ≥400

### 12.4 L1 大白话重述 (dad 说「没理解」)

> 卡片中间那块「画框」是**长的**（宽是高的 1.22 倍），但徽章那张画是**正方形**的。
> 正方形塞进长条框里，左右两边各空出一条约 41px 的底 —— 空的地方就是卡片本身的底色，
> 深色主题下看着像画面两侧各多了一道暗边。三个选择：
> **A** 把画放大到填满框（左右补满，但上下会被裁掉约 9%，头顶的标带可能被切）；
> **B** 把框改成正方形（左右不再有空，但整个画面变小、卡片更"瘦"）；
> **C** 左右空出来的地方不露卡身，改成主题色暗纹底（画面大小不变，只是把"空"变成"设计"）；
> **D** 先不动（dad 已验收过这版，肉眼看不出问题就不动）。

### 12.5 L5 接生产方案 (防伪纹)

1. **强度**：`--ccg-sec-gain` 已经是单点旋钮 —— 生产端不需要改 CSS，只把选定的档位写进卡面即可。
   建议三档语义：弱 ×1.0（现值）/ 中 ×1.6 / 强 ×2.4；dad 选一档就固定为全站默认。
2. **纹样**：现在 7 套深色主题各自一个 SVG 暗纹（`static/img/ccg-patterns/`），淡色主题共用默认暗纹 ⇒ 若要"每卡可配纹样"，加 `achievements.card_sec`（可选列，取值 = 纹样 id），未设时按 `card_theme` 兜底（与 `card_theme`/`card_stars` 同一套 resolve 模式）。
3. **接线**：领取弹窗 `_badge_claim_modal.html` 挂 `data-ccg-sec` / `data-ccg-sec-gain`（跟 `data-ccg-theme` 同路径），三处 payload（`badge_claim.py` / `minip_api.py` / `app.py` 副本）各加一个可选字段。
4. **登记**：属 🟡 部分兼容（新增可选字段 + 新增 DB 列），改完更新 `API-CHANGELOG.md` + 评估小程序同步。
5. **风险**：强度调高会让暗纹在**小尺寸缩略图**（小程序列表 60px）上变成噪点 ⇒ 接生产前需在 60/120/240px 三档尺寸各看一次；必要时按尺寸分档（CSS `@media` 或 `container-query`，无需新列）。

---

## 13. l1 实施: 镭射铺满整卡 + 外框简化 (v1.2.0-dev, 2026-09-13)

### 13.1 dad 原话拆解 (5 个动作项)

> 「徽章那张画的 3d 镭射背景 我希望能整个铺满卡片…铺满后下面文字部分要根据不同主题的配色做对应的对比展示可视程度的处理…我能数到的一个是最外面的金环、一个是整体卡片背景那本边缘、一个是 badge 所在画面的镭射背景…等于去掉一个整体卡片背景了。然后，外面的金环 11px 太粗了 减少。最外围的外框太复杂了」

1. 镭射/防伪箔层**从画面窗搬到卡面级 → 铺满整卡**（顺带消掉「主体左右各 ~41px 裸卡身」那个老问题）
2. 铺满后**底部文字区按主题做对比可视处理**
3. **去掉一层圆角矩形**（画面窗自己那圈镭射底 + 压暗边）
4. 金环 `--ccg-rim-w` **11px → 6px**
5. 最外围外框**从 5 环简化到 3 环**

### 13.2 实施 (文件 / 落点)

| 文件 | 改动 |
|------|------|
| `badge-ccg.js` | `artInner()` **只出主体**（不再在窗内出箔层）；`frontMarkup()` 出**两组卡面级箔栈**：`.ccg-foil-stack`（背景组 = stock + shine + glitter + security，z1）→ 主体（z5）→ `.ccg-foil-stack.ccg-foil-over`（流光组 = glare + spec + laser，**z6**，压在主体之上但在文字区 z7 之下） |
| `badge-ccg.css` | `.ccg-art-window` **删 `overflow:hidden` / `isolation`**，退化为纯定位容器；**删 `::after` 四边压暗**（= dad 数的第二个圆角矩形）；`.ccg-frame` box-shadow **5 环 → 3 环**（1px 暗掐丝 + 4px 主金 + 1px 内高光，总宽 `var(--ccg-rim-w)`=6px）；`.ccg-frame::after` padding 11px → `var(--ccg-rim-w)`；`--ccg-rim-w: 11px → 6px`；新增 `.ccg-foil-over { z-index: 6 }` |
| `badge-ccg.css` | 新增 `--theme-foot-scrim`（页脚底衬渐隐，深色极性）+ 说明栏默认 scrim 加厚（0/0.30/0.58 → 0.06/0.46/0.78）；`.ccg-foot::before` 承载渐隐（**零几何影响**，页脚位置不动） |
| `badge-ccg-themes.css` | pearl / mint / sakura 各加一行亮极性的 `--theme-foot-scrim`（与 `--theme-plate-scrim` 同极性逻辑） |
| `badge-ccg-compare.html` | ① 金环标注带宽改为读 `var(--ccg-rim-w)`（自动跟随）；② 文案 2px → 1px（底座） |
| 版本号 | `badge-ccg.css:1` / `badge-ccg.js:1` / demo 页 chip + `<meta>` + `window.CCG_DEMO_VERSION` = **v1.2.0-dev**；`demo-archive/VERSIONS.md` 加行 |

### 13.3 机械自检 (Chrome CDP 9445, 只取数值; 视觉交 dad)

- 前端每张卡 **2 个卡面级箔栈**，`bgCover=true` / `overlayCover=true` —— 箔栈包围盒与卡片包围盒**逐值相等**（324×481.9）⇒ 真·铺满整卡
- `.ccg-art-window` 保留（定位用，286.4×244.5），但 `overflow=visible`、`::after boxShadow=none` ⇒ **窗口不再成框**
- 金环：`--ccg-rim-w: 6px`；`box-shadow` = `1px #342205 inset + 5px #d4a017 inset + 6px #ffeaa0 inset`（**3 环**，旧版 = 5 环到 11px）
- 左缘逐 0.5px 采样（azure，卡宽 642px@2x）：`0~1px 暗掐丝 → 1~5px 主金(212,160,23) → 5~6px 内高光(238,222,162) → 6px 起卡面箔底(130,142,179)` ⇒ 环宽实测 6px 收口正确
- 文字区对比度（WCAG，8 主题；底 = 文字行区域中位色）：说明栏 **7.77–12.03**，页脚 **5.65–6.73** ⇒ 全部 ≥ AA 4.5
- 两页 console **0 错误**；服务端 md5 对盘一致；冻结旧版页 v1.0.0 仍可打开（对照用）

### 13.4 主题语义澄清 (dad 2026-09-13)

> 「这几套主题不是为了前端能让用户选择展示的，而是为了我作为每次设计卡片时的选项，卡片设计完就成型了，以后要改也是我自己在 config 里面改」

- **不做**用户端主题选择器（现状即符合：卡面主题由后端 `achievements.card_theme` 决定，一张卡一个值）
- 8 套主题 = **dad 的设计素材库**；demo 页的下拉 = 设计工具，不是产品功能
- config 侧「改这张卡主题」的编辑入口**目前不存在**（只能改 DB）→ 登记为 backlog **B2**

## 14. dad 二轮两处修改 (v1.3.0-dev, 2026-09-13)

### 14.1 dad 原话

> 「1.mouse 移动时跟随的那个光照太强烈了，直接会让图案都看不清楚，可以适当减弱 2.ccg-foot 为什么不是和 ccg-plate 放在一起的？要改为在一起的，就是说 ccg-foot 是在 plate 里的，ccg-foot 的自己的背景不需要了，也就是说 ccg-foot 里面的星级和右边的 dizical 标志放进 plate 里。其它前端不要改了都很好，记住就动就这两个」

### 14.2 改动 (只落这两处)

| # | 落点 | 改动 |
|---|------|------|
| ① | `badge-ccg.css` 新增 `--ccg-light-gain: 0.6` | **跟随光标的聚光灯总强度旋钮**，`glare`（柔光）+ `spec`（主光）两层 opacity 同乘。旧值 = `1`，现默认 `0.6` ⇒ **减弱 40%**。`laser`（镭射彩虹）与 `security`（防伪）**不在旋钮内**（dad 认可镭射，只嫌光照冲淡图案）。背面 `.ccg-back-face .ccg-foil-glare` 同乘（同一类跟随光照） |
| ② | `badge-ccg.js` `frontMarkup()` | `.ccg-foot`（星级 + DIZICAL）**移进 `.ccg-plate` 内部**，作为说明栏最后一行；`.ccg-foot` 不再有 `position:absolute` / 自己的背景 |
| ② | `badge-ccg.css` `.ccg-plate` | `bottom: calc(100% - var(--plate-b))`（88%）→ `bottom: var(--foot-b)`（3.2%）；`padding` 底 2.6% → 0 ⇒ **说明栏底衬一条延伸到卡底页脚**，`--plate-b` token 废除 |
| ② | `badge-ccg.css` `.ccg-foot` | 改静态 flex 行：`width:100%` + `margin-top:auto`（顶到栏底 = 原绝对定位位置）；`border-top` / 字号 / 墨色 / `span:last-child` accent 全部保持 |
| ② | `badge-ccg.css` `.ccg-foot::before` | **删除**（=「自己的背景不需要了」）；`--theme-foot-scrim` 一并删除（含 themes.css pearl/mint/sakura 三处覆盖）—— 文字区底衬统一由 `--theme-plate-scrim` 负责 |

### 14.3 机械自检 (Chrome CDP 9445 + PIL，只取数值；视觉交 dad)

**① 光照 (同一指针位、同一页面，只切换 `--ccg-light-gain`)**

| 层 | gain=0.6 (新) | gain=1.0 (旧) | 结论 |
|----|---------------|---------------|------|
| `.ccg-foil-glare` | 0.60 | 1.00 | ×0.6 |
| `.ccg-foil-spec` | 0.54 | 0.90 | ×0.6 |
| `.ccg-foil-laser` | 0.54 | 0.54 | **不变** |
| `.ccg-foil-security` | 0.7706 | 0.7706 | **不变** |

插画区（画面窗，2× 截图）像素统计：

| 指标 | 新版 gain 0.6 | 旧版 gain 1.0 | 变化 |
|------|---------------|---------------|------|
| 平均亮度 | 191.5 | 220.7 | −13% （旧版已接近白） |
| p90 高光 | 224 | 250 | 旧版高光贴顶 = 细节丢失 |
| 边缘能量（细节量代理） | 4.05 | 3.03 | **+34%** ⇒ 图案更清楚 |

**② 页脚并入说明栏**

- DOM：`document.querySelector('.ccg-plate').contains(document.querySelector('.ccg-foot'))` = **true**；`.ccg-foot` 子节点 = `.ccg-stars` + DIZICAL span
- `.ccg-foot` `background-image` = **none**；`::before` `content` = **none** ⇒ 无自带背景
- 几何（卡正面前提）：说明栏 top **67.85%**；说明栏 bottom 距卡底 **3.35%**，页脚 bottom 距卡底 **3.38%**，两者差 **0.13px** ⇒ 栏底与页脚底重合、页脚**视觉位置未变**
- 说明栏只 1 层渐隐（`background-image` 96 字符 = 单层 linear-gradient）
- 页脚行对比度（说明栏底衬 100% 处合成色 vs 页脚墨色）：深色 5 套 **9.79–12.88**，淡色 3 套 **5.81–6.58** ⇒ 全部 ≥ AA

### 14.4 版本

`v1.3.0-dev`（v1.2.0-dev 未冻结、已并入）；四处同步：`badge-ccg.css` / `badge-ccg.js` / demo 页 / 对比页 + `VERSIONS.md` 加行。**未动**：后端 Python、画面主体、金环、底座、防伪档位、其它页面。

