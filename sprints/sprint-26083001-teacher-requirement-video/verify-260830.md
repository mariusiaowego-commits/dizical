---
title: Sprint 26083001 — verify closeout (S1-S6 全部完成)
tags: [dizical, sprint-26083001, verify, closeout]
source: ai-agent
status: 已完成 (真机跨端由 dad 自走验收)
updated: 2026-08-30
---

# Sprint 26083001 — verify closeout (3-1-1)

> Sprint: [sprint-26083001](/05-Coding/project-dizical/sprints/sprint-26083001-teacher-requirement-video/sprint-26083001-teacher-requirement-video.md)
> 状态: **已完成** — S1-S6 全部 merge main + 云端 deploy #111 上线, 跨端由 dad 自己验收。

## What shipped (3 bullets)

1. **老师要求示范视频全链路**: S1 后端 (commit `355e83c`) — `cos_client.upload_stream` 流式上传 + `POST /config/api/assignments/upload-video` (后端硬校验 200MB + 白名单 mp4/mov + 413) + `weekly_assignments.videos` JSON 列 (SQLite PRAGMA + MySQL 迁移脚本幂等) + `_serialize_assignment` 带 videos 防 PUT 静默清空。
2. **三处前端入口**: S2 (commit `f3376fb`) — 录入/编辑/prepare 视频区 (XHR 流式上传 + progress + 绑定下拉「本表 item / 本期通用」+ localStorage 草稿) + S3 (commit `796bd5b`) — practice 页「老师示范」chip + modal `<video controls playsinline webkit-playsinline>` + webkitendfullscreen 状态机同步 (grok 拍板 B: 不调 closeVideoPreview)。
3. **云部署 + 修复**: S5 (commit `8b280a5`) — PooledDB `ping=7` 自动 reconnect 解决 CloudRun 容器唤醒时 MySQL 死连接 2013 500 + sync 脚本 exclude `_worktrees` (部署包 1.3GB → 162MB 8 倍瘦身) → Deploy #110 (commit `f4036d8`); S6 — 音乐字符输入框统一渲染 (font-feature-settings calt/ccmp/mark/mkmk) → Deploy #111。

## 1 thing learned

- **双线 audit (agy + deepseek) 是 S5 上线前关键防御**: 单视角会漏盲点 — MySQL ping=7 由 agy 后端视角发现, `_worktrees` 1.1GB 由 deepseek 文档视角发现。两者叠加避免 100% create_failed 部署失败。**结论**: 上云前默认派两 agent 做不同角度 audit。

## 遗留 (留给下个 sprint)

- [ ] 32MB body size 解决方案 — 视频 >32MB 上传会被 CloudRun 网关拦, dad 接受现状 (微信视频 ≤30MB); 未来需前端直传 COS (S4 列出 3 方案)
- [ ] 跨端验收 (iPhone/iPad/Mac/WKWebView) — 由 dad 自走, 暂未做
- [ ] git tag v1.0.X — Sprint 26083001 累计功能 tag

## 部署清单

- Deploy #110 (S1-S5 上线, 镜像 `dizical-prod-110-20260830134342`)
- Deploy #111 (S6 字符渲染修复, 镜像 `dizical-prod-111-20260830144005`, 当前线上)
- 公网 URL: https://dizical-prod-283401-10-1454535414.sh.run.tcloudbase.com
- MCP 警告: VpcConf 缺失 (MySQL 走公网 OK, 已知不阻塞)