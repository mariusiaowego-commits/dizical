---
source: ai-agent
date: 2026-09-20
project: dizical
doc_type: 决策记录（澄清结果）
related: [MRD-practice页重构.md, practice页重构-待澄清问题-260920.md]
status: 已澄清（2026-09-20）
tags: [dizical, practice, refactor, 决策记录]
---

# practice 页重构 — 决策记录（澄清结果 260920）

> 来源：dad 在 `practice页重构-待澄清问题-260920.md` 里逐条写下的回答 + 追加补充。
> 本文把「你的答案」翻译成「执行口径」；**执行口径写错或理解偏了，直接改本文**。
> 未定的点集中在 §6。

---

## 0. 本次目标（照 dad 的原话提炼）

重做 practice 页**整体板块 UI 风格**：整体板块 UI 风格 + 统一卡片元素风格 + 模块排布 + 动线。
产出目标是一个**可配置、模块化、能换配色主题**的 practice 练习 dashboard。
讨论方式：**基于 demo 讨论**，一个个模块过 UI / 样式 / 交互。

dad 追加的 5 个参考（inspora.design）：
1. `interactive-cards` 沉浸式卡片
2. `tracking-cards` 列举卡片
3. `product-comps` 进度卡片
4. `1-5` 日历点击后的下方关联卡片
5. `1-35` 打印 receipt 的动效

---

## 1. 澄清结果 → 执行口径

| # | 主题 | dad 的答案 | 执行口径（我按此做） |
|---|---|---|---|
| Q1 / D2 | 主视觉深度 | 本期做 A（2D 主视觉），后续可能需要 3D 模型场景 | 本期只做 2D（刻度尺时钟 + 节拍器视觉）；3D 留位置与容器契约，进 Phase 2 |
| Q2 | 节拍器是否发声 | 做 C：先视觉；声音单独拆分支专门设计，dad 会提供参考 | 本期节拍器 = 纯视觉（跟 BPM 摆动/闪烁）；发声另开分支 |
| Q3 | 老师要求录入格式 | 不是脏数据；有些曲子就是有多种节拍要求。要求：①先列出 `requirement` / `requirements` 分别是什么 ②音符漏写要在提交前验证 ③按他给的 5 字段重建 metronome | 见 §2（已出对照结果）；数据模型按 `section` / `beat_unit` / `bpm_min` / `bpm_max` / `notes` 设计，`bpm_max` 必填、`bpm_min` 选填 |
| Q4 | 老师要求卡片可见性 | **要一直可见**；练习中也一直展开显示原文；字要大、排版清晰、重点数据突出；**单独做 demo** | 要求卡片 = 常驻模块，练习态不收起；单列一个模块 demo |
| Q5 / D3 | 音符档位 | A；**没有 32 分音符，只到 16 分音符 ♬** | 三档：♩（四分）/ ♪（八分）/ ♬（十六分）；不用补充区字形 |
| Q6 | 开始按钮必填 | A | 保留必填 + 选中科目自动预选第 1 个标签并填入 + 灰的时候在按钮下写原因 |
| Q7 | 孩子 / 家长视图 | A | 分视图：孩子视图藏掉归档、编辑、删除等家长动作，只留选科目 + 计时 + 打卡 |
| Q8 / D1 | 科目选择 | ①归档科目**不能过滤**，但要明确标出"已归档"，用户可去专门板块找 ②默认优先选**老师要求里的科目**，其次按最近练过 ③具体形态等 agy 卡片方案定 | 科目区默认序：本周老师要求科目 → 最近练过 → 其他在用；归档科目单独入口/分组并带归档标识 |
| Q9 | 标签极限态 | 按 A 看效果 | 超过 6 个换行，最多 2 行，再多收进"更多"；兜底标签是否保留待定（§6） |
| Q10 | 今日练习记录位置 | ①若是一屏流 dashboard，就要折叠或点开 modal ②先看整体布局再定位置 | 先按"可折叠 / 可弹 modal"设计卡片本体；位置等整体布局定 |
| Q11 | 结束与打卡 | A：都是一个模块，都算练习 flow 里的模块 | "结束与打卡"作为独立模块一起过 demo |
| Q12 | 无障碍欠账 | A：一起清 | 触控热区 ≥44px、焦点环恢复、放开双指缩放、对比度问题一并处理 |
| Q13 | demo 分工 | A：按分工一个个出；不满意 dad 随时点名换前端 agent | 严格模块化推进，每个模块一份可点 demo，dad 过了再下一个；**前端不给 minimax** |
| D2 | 3D | A + **后续方案与计划先建一个 sprint-workflow 占位，写明计划** | 见 §5：已建 sprint 占位（含 3D Phase 2 计划） |
| D4 | 首屏原则 | **一屏闭环**；并且**绝大部分时间女儿是 iPad 横屏**；practice 要是可直接操作的 dashboard，后续还要 3D 沉浸式，所以必须保持一屏 | 主视口从"iPad 竖屏"改成 **iPad 横屏 1133×744**，一屏内闭环：选科目 → 看要求 → 定速度内容 → 开始（不滚动） |

**⚠️ 这条改动的影响面最大**：主设备与朝向从"iPad mini 竖屏 744×1133"改成"**iPad 横屏 1133×744**"，加上"一屏闭环"约束 → 所有模块的尺寸预算（卡片高度、字号、留白）都要按横屏 744 高来排，竖屏 744×1133 与 iPhone 440×956 作为次视口。

---

## 2. Q3 的字段调查结果（dad 要求先列出来）

`weekly_assignments.items` 是 JSON 数组，每条是 `{item, item_id, metronome, requirement 或 requirements}`。全量 29 条记录（2025-11-08 ~ 2026-08-01）实测：

| 键 | 出现次数 | 时间跨度 | 与另一键同时出现的次数 |
|---|---|---|---|
| `requirement`（单数） | 49 | 2025-11-15 ~ **2026-05-09** | **0** |
| `requirements`（复数） | 57 | 2025-11-08 ~ 2026-08-01 | **0** |

**结论 1**：两个键**语义相同**（都是"要求正文"），是前后两版录入实现留下的键名差异，**按时间严格分段、从不混用** —— 所以读取端做 `requirement || requirements` 兜底就够，不需要做语义区分。
**结论 2**：分界点在 **2026-05-16**：五月之前写单数、之后写复数（唯一例外是 2025-11-08 那条复数）。

**顺带发现（比键名更要紧）**：早期记录（2025-11 ~ 2026-05）里 `item` 字段不是科目名，而是分类词 —— `要求` / `作业` / `大课` / `Test`，而且大多没有 `item_id`。也就是说这批老数据**无法和科目关联**。设计时要么不显示这批，要么单独放"历史要求"区。**这条要 dad 定**（§6-1）。

**你给的 metronome 结构化方案（我按此建模）**：

| 字段 | 含义 | 取值 / 规则 |
|---|---|---|
| `section` | 切片 / 段落 | 填哪个段落或片段（自由文本） |
| `beat_unit` | 拍值基准 | 选择：♩ / ♪ / ♬ |
| `bpm_min` | 速度下限 | 选填 |
| `bpm_max` | 速度上限 | **必填**（只有一个速度时填这一栏） |
| `notes` | 备注 | 如"前 3 天练 A 速度，后 4 天练 B 速度" |

这解释了为什么"多种节拍要求"不是脏数据：一个科目在一个 stage 里可能同时有 4/4 ♪=80 与 2/4 ♪=69-80、或 tu/ku 两个速度 → 数据结构天然要支持**一个科目多条速度段**。
你另给了两个速度录入交互参考：Pinterest pin `628604060472112191`、`112097478221225231`。
"音符漏写"这条按你的要求变成**提交前校验**（beat_unit 必选）。

---

## 3. 由此改变的既有前提（重要，避免按老前提返工）

1. **主视口改横屏**：iPad 横屏 1133×744 为主，一屏闭环；竖屏与 iPhone 为次。
2. **孩子 / 家长视图分离**：卡片里不放归档、编辑、删除等家长动作。
3. **无障碍一起清**：允许双指缩放、± 键与所有触控目标 ≥44px、焦点环恢复、对比度欠账处理。
4. **结束与打卡**纳入本次模块清单（属练习 flow）。
5. **节拍器**本期只做视觉；发声独立分支（dad 会给参考、要做完整 feature）。
6. **3D 沉浸式**进 Phase 2，先建 sprint 占位写明计划。
7. **补录**已砍掉（历史使用 4.9%，最近一次 2026-06-13）。
8. **纪律**：前端不给 minimax；模块化逐个过 demo；必要时可开 1-2 个 agy（Claude）pane 做复杂前后端的深度分析。

---

## 4. 模块清单与 demo 分工（Q13 表的落地版）

| 序 | 模块 | demo 形态 | 谁写 | 依赖 |
|---|---|---|---|---|
| 1 | 卡片基础规范 + 状态矩阵 | 对照页（默认/选中/禁用/焦点/长文/空态） | agy 出方案 → Hermes 写 | 无 |
| 2 | 选科目 | 可点 HTML | Hermes | 1 |
| 3 | 老师要求（常驻 + 练习态） | 可点 HTML（175 字长文 + 多速度段极限态） | Hermes | 1 |
| 4 | 速度 + 本次内容 | 可点 HTML（三档音符 + 9 标签极限态） | agy | 1、§2 字段 |
| 5 | 刻度尺计时器卡片化 | 可点 HTML（复用生产真件） | Hermes | 1 |
| 6 | 结束与打卡 | 可点 HTML（正常/提前结束 + 失败重试） | agy | 1 |
| 7 | 今日练习记录 | 可点 HTML（折叠 + modal 两态） | agy | 1、整体布局 |
| 8 | 顶部状态 / 页脚 / 主视觉 | 可点 HTML + 动效页 | Hermes | 1 |
| 9 | 3D 沉浸式主视觉 | 计划占位（Phase 2） | 待定 | sprint 占位 |

---

## 5. 已建的 sprint 占位（响应 D2）

- 主仓：`sprints/sprint-26092004-practice-ui-refactor-2026-09-20/`
- Obsidian 镜像：`tqob/05-Coding/project-dizical/sprints/sprint-26092004-practice-ui-refactor-2026-09-20/`
- 内含：sprint 主档 stub + plan（含 3D Phase 2 计划占位）+ tech-spec（待实现后回填）

---

## 6. 还等你定的 3 个小点（2026-09-20 已答）

1. **老数据（2025-11 ~ 2026-05，item 是"要求/作业/大课"、无 item_id）**：dad 答 —— 算作**历史要求**（单独区展示，不当练习科目）
2. **Q9 兜底标签**：dad 答 —— **保留**（长音练习 / 吐音练习 / 连吐练习 / 换气练习）
3. **Q10 位置**：dad 答 —— 等整体布局出来后再安排（本文 §4 的模块 7 位置暂留空）

## 8. 复核 agy v2 后追加的 4 条拍板（2026-09-20）

| # | 问题 | dad 拍板 | 执行口径 |
|---|---|---|---|
| A1 | 老师要求区装不下 175 字极限态 | **原定"常态 260px + 超 6 行弹全屏阅读层"；2026-09-21 dad 修正：高度不写死** | 要求卡**高度内容自适应**（min-height 仅做视觉节奏，不设上限）；只在"内容确实超出一屏预算"时才收住并出现「展开全文」→ 全屏阅读层（练习中可呼出，关闭不停表） |
| A2 | 归档科目的视觉标识 | **灰底 + "已归档"药丸，不加删除线**，排第四顺位分组 | 归档卡：灰底背景 + `已归档` 药丸；**不加删除线**；默认折叠在"已归档 (37)"分组里 |
| A3 | 一个科目有多条速度段时默认填哪个 | **默认回填第一条的 `bpm_max`，并显示"还有 N 段"可切换** | 速度组件默认取第一条速度段的 `bpm_max`；旁标"还有 N 段"；点开可切段（段位来自 `section` / `notes`） |
| A4 | Phase 2 的 3D 主视觉容器 | **先不定，等 Phase 2 设计时再说** | 本期只保证右栏顶部 240px 环境层的**位置契约**，不写死 3D 交互 |

> 这四条已作为模块 1（卡片规范）与模块 2/3（选科目 / 老师要求）的输入。

## 10. 布局方向升级：两态两套布局（2026-09-20 dad 追加）

dad 原话要点：
1. **老师要求卡与仪表卡同宽**，布局上**关联在仪表卡下方** —— 开始练习后能边计时边看这个科目的本周要求
2. 界面分**两种状态**，模块排列完全不同：
   - **准备态**：决定 选什么科目 / 什么速度（BPM）/ 练什么 session / 练多久（刻度尺移动选时长）
   - **练习态**：点开始后进入，**围绕一个完整的中央仪表盘**展示与交互；刻度尺变成计时器（花朵生成动画 / 刻度移动 / 时间动效**保留**）；BPM、已选 session 等成为「进行中的仪表卡」组成部分，要素丰富、排列统一和谐；下方关联老师要求（字大、结构化）；还要安排「今日练习记录」板块
3. 由此**取代** agy v2 里的"左信息 350 / 右主控 667 固定双栏"作为唯一布局（准备态可沿用双列，练习态改为围绕仪表盘）

产出：
- Hermes 草案 `docs/practice页重构-两态布局草案-hermes-260920.md`（准备态双列 / 练习态 R1-R3 / 今日记录 A-C / 高度预算算术）
- 已派 agy 做对照脑爆（挑错 + 各态 2 候选 + 练习态要素清单 + 今日记录摆法）
- 真件可点页：`docs/demos/practice-instrument-260920.html`

## 12. dad 2026-09-22 深度反馈（v2 评审 → v3 需求，逐条）

| # | 问题 / 要求 | 执行口径 |
|---|---|---|
| B1 | **状态切换太硬**（"计时态不要重新刷新页面出来"） | 改成**模块级双态**：不重新渲染整页；每个模块自己有两态；切换时"该状态用不到的模块"加**灰色蒙版 + 锁定感**；**整体布局不因状态变化大幅变化** |
| B2 | 选科目区**边框套边框** | 卡片层级收敛：外层卡不再套内层卡边框；用留白 + 分组标题分层 |
| B3 | 模块标题太随意（"1 选科目"） | 标题交给 agy 润色；每个模块一个像样的名字（不许用编号当标题） |
| B4 | 速度与本次内容：胶囊下面又换行出数字填写 → **layout 太低级** | 按 `better-ui` / `better-layout` 重学排布；分段胶囊与数值/步进必须同一行、同视觉系统，不许换行成两层 |
| B5 | 仪表卡（计时器卡）**关键信息不在视觉重点**；反而把我自造的"吹奏保持专注 · 笛声平稳充沛"放到了视觉中心 | **先确认重点内容，再做锦上添花**：科目 / 速度 / session 必须是仪表卡的视觉重点；自造文案删除；数字排布参考 `inspora.design/posts/product-comps`。**刻度尺动画与交互不许动**（拖拽 / 红针 / 卷草纹 / 气泡），但仪表卡内部 UI 排列可以重做 |
| B6 | 左侧「选科目 + 速度 + 本次内容」应是**一个整体** | 合并成**一整条长条形卡片**（单 card 内部合理排布 + 多设备适配）；每科目先放一个**沉浸式 heroic 占位图**（后续生图替换）。样式参考 `科目卡片参考-01.jpg`（hero 占 ~40-50% 高 + 主标题居中 + 信息网格 + 细分割线）与 `科目卡片参考-02.jpg`（hero 占 ~55-65% + 品牌/标题层 + 底部大字数值 + 标签数值网格） |
| B7 | **练习态的进入动画**（概念构想，要补完整） | 点「开始练习」→ 那张长卡片**快速变淡变透明**；同时**计时器处插入这张卡片**（像打卡机打卡）：插入动画**不展示完整卡片**，只露出卡片最顶部一点（沉浸 hero 图 + 卡片头部），孩子靠这一点点露出的头部辨识"现在练的是哪张卡"；插入完成后仪表盘**隐现这次练习的关键内容**（科目 / 速度 / session） |
| B8 | 参考要逐帧分析 | `inspora.design/posts/1-5`（点击后的下方关联卡片）、`/posts/product-comps`（仪表卡数字排布）、`/posts/interactive-cards`（交互与动效） |

> B5 是本轮最重要的一条：**先确定"重点信息是什么"，再谈美化**。不许再往视觉中心塞自己想的文案。



---

## 13. 附：`requirement` vs `requirements` 现状结论（2026-09-20 查代码 + 查库）

| 项 | 结论 |
|---|---|
| 语义 | **没有区别**，同一个"要求正文"的两个键名 |
| 单数来源 | 录入端写的：CLI 老师要求解析（`src/cli.py:1269/1273/1283`）与表格导入（`src/practice.py:522-547`）传的是 `requirement` |
| 复数来源 | **落库时被规范化**：`src/database.py:727-733` `save_weekly_assignment()` 明确把 `requirement` 改写成 `requirements`，并保证 `requirements` / `metronome` 键存在 |
| 现在最新在用哪个 | **`requirements`（复数）** |
| 数据侧证据 | 复数最早 2025-11-08、**一直到 2026-08-01（最新一条）**；单数最后出现在 **2026-05-09**；两键从不混用 |
| 读取侧 | 所有读取点都是 `requirements or requirement` 兜底（`app.py:1450/2000/2593/2657/2667`、`config.py:1050/1081`、`practice.py:294`、`cli.py:1098`）→ 现状不会出错 |
| 结论 | 老的单数记录是**规范化逻辑上线之前**写进去的残留；**前端读取照旧兜底即可**。要不要清历史那 49 条（一次性幂等迁移把 `requirement` 改写成 `requirements`，跑完给前后对比 + 审计）—— **待 dad 定** |

---

## 14. v10 修订（2026-09-23 dad 反馈七条 + 重复点拍板）

票卡文件：`docs/demos/practice-ticket-v4-260920.html`（本轮定性 = **重新设计**，不是缩高度）

| # | dad 原话要点 | 执行口径 |
|---|---|---|
| 1 | "线条与线条到处布防…满眼望去全是线条…不能光缩小高度，而是要重新设计 ui" | 先交**线条盘点表**（每条线：位置/类型/作用/判定删·并·留）；按 better-layout 删除可由间距承担的线；组内 8-12 / **组间 ≥16 且 ≥2× 组内**；一屏仍达标但靠重排+删线，放不下则**减少票面信息** |
| 2 | "数字用衬线字体发现不行，改回去" | 速度 / 时长数字**改回 monospace + tabular-nums**；DESIGN.md 已同步回退（commit `fb1bd1b`） |
| 3 | `pk-ticket-subject-title` 衬线但粗体 | 保留衬线字族，**字重 600-700** |
| 4 | `pk-stub-memo-block` 像装饰且不美；"dizical 这种字符就不要出现第二次" | 删重复 kicker 文案（全票 `dizical` 仅 1 次）；改由 better-layout 重排 |
| 5 | "按钮要去和刻度尺/计时器模块放一起" | `#pkStartBtn`（含 `#pkWhy` 禁用原因）**移出票卡** → 右侧仪表模块控制总线；票卡底部不再有主按钮 |
| 6 | 缺角处灰线仍直穿，不像凹圆角 | 边框须在缺口处**中断并沿半圆绕行**（mask/clip + 1px 半圆弧补描边）；禁止"贴圆片"；左右对称 |
| 7 | `pk-ticket-hero-tag` 不够透明 | 80% → **50%** |
| — | **重复点拍板** | dad 选 **方案 B**：右侧 `pk-req-card` **保留老师要求全文**；票卡备注区**不放要求正文**，只放**档案编号 + 科目分类** |

其他既定约束不变：真件 7 id 不改名 / 刻度尺动画交互一行不改 / 只 dizicute 6 色 + alpha / 触控 ≥44px / 卡片内不换行 / 无 emoji 无外链无 alert / 两态不叠加 / session 面板向上展开不撑高票卡 / 只用生产库真实文本。
