# Handoff — 2026-05-12

## 本次会话完成事项

### 1. SQLite 并发问题修复（严重）
- **文件**: `src/database.py`
- **问题**: `unable to open database file` — 多线程并发 `connect()` WAL 数据库，macOS 上偶发
- **修复**: 
  - `_get_connection()` 改为单连接复用 `self._conn`
  - `check_same_thread=False`
  - 去掉 WAL journal mode（macOS 兼容问题）
- **服务启动**: `uvicorn src.kid_app.app:app --port 8765 --host 0.0.0.0 --workers 1`
- **进程 ID**: `proc_7c76369bf809`（当前运行中）

### 2. achievements.html 布局重构
- **文件**: `src/kid_app/templates/achievements.html`
- 去掉 2 列 grid，改为单列全宽卡片（`margin: 0 12px 12px`），与其他 tab 一致
- 所有 GSAP 动画适配新布局

### 3. 本周目标天数修复
- **文件**: `src/kid_app/app.py`
- `_week_progress()`: `goal = 5` → `goal = 7`
- fallback: `"0/5 天"` → `"0/7 天"`

### 4. Badge 生图（20 张）
- **目录**: `src/kid_app/static/badges/`
- 全部 20 个 badge 图片通过 Hermes Nous subscription 生成并下载
- 文件列表: streak_1/3/7/14/30/100, total_60/300/600/1000, first_log, double, week_champ, full_month, all_items, night_owl, one_breath, comeback, song_end, top1/2/3, 另有旧文件 fire_badge/medal_badge/star_badge/week_star_badge

### 5. 前端+后端接入 badge 图片
- **文件**: `src/kid_app/app.py`
  - `_milestone_html()`: emoji → `<img src='/static/badges/{id}.png'>`，用 `_badge_img()` 辅助函数
  - `badges_page` → `render_badge_item()`: emoji → `<img>`
- **文件**: `src/kid_app/templates/achievements.html`
  - more-card: emoji → 真实 badge 图片

### 6. badges.html 网格优化
- 手机 2 列（原来 3 列太挤）
- 平板 3 列（500px+）
- 大屏 4 列（800px+）
- gap 14px，padding 减少

---

## 当前服务状态

```
进程: proc_7c76369bf809 (uvicorn, --workers 1)
端口: 8765
访问: http://mt16demacbook-pro.local:8765
路由:
  /achievements — 200 OK
  /badges      — 200 OK
```

## 待办事项（未完成）

1. **勋章可配置化** — 后续 praise tab 里增加勋章配置功能（用户要求）
2. **幽默描述优化** — 5 个 milestone 当前用通用描述，待改为俏皮得瑟语气
3. **README 更新** — PR 前按惯例必须更新文档
4. **Git 提交** — 本次修改未 commit，需 `git add` → `git commit` → feature branch → PR

---

## 数据库

- 路径: `/Users/mt16/dev/dizical/data/dizi.db`
- 连接修复: 单连接复用模式，避免 macOS WAL 并发问题

---

## 技术笔记

- macOS 上 SQLite WAL + 多线程 `connect()` 有兼容性问题，单连接复用是稳定方案
- `--workers 1` 避免 uvicorn 多进程竞争同一连接
